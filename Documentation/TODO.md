#  TODOs


## Loggers

[] In Logger maybe change: do something like:
    LoggerBase (with all the base)
    LoggerDev (with logDev, logPreview)
    And Logger will have all.
    Or maybe Logger will be the base ...
